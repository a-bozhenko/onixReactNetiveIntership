const LIST_TYPES = {
  SECTION_LIST: 'sections',
  LIST_ITEMS: 'items'
};

const COLOR_SCHEMES = {
  AUTO: null,
  FROM_DEVICE: 'device',
  LIGHT: 'light',
  DARK: 'dark'
};

export { LIST_TYPES, COLOR_SCHEMES };
